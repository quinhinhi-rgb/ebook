# ebook# 馃摉 G茅n茅rateur d'Ebooks Illustr茅sUne petite appli web qui g茅n猫re un ebook (texte via **Claude**, illustrations via **DALL-E**) et le t茅l茅charge en **PDF**.## 馃梻锔� Structure```ebook-generator/鈹溾攢鈹€ netlify.toml               鈫� configuration Netlify鈹溾攢鈹€ package.json鈹溾攢鈹€ public/鈹�   鈹斺攢鈹€ index.html             鈫� l'interface (front-end)鈹斺攢鈹€ netlify/functions/    鈹溾攢鈹€ generate-text.js       鈫� appelle Claude (Anthropic)    鈹斺攢鈹€ generate-image.js      鈫� appelle DALL-E (OpenAI)```Tes cl茅s API (Anthropic, OpenAI) restent **c么t茅 serveur** (dans les fonctions Netlify) : elles ne sont jamais visibles par les visiteurs de ton site.---## 馃殌 D茅ploiement pas 脿 pas (aucune comp茅tence technique requise)### 1. Mettre le code sur GitHub1. Cr茅e un compte sur [github.com](https://github.com) si tu n'en as pas.2. Clique sur **"New repository"**, donne-lui un nom (ex : `ebook-generator`), clique **Create repository**.3. Sur la page du nouveau repo, clique **"uploading an existing file"**.4. Glisse-d茅pose **tous les fichiers et dossiers** de ce projet (garde bien la structure des dossiers `public/` et `netlify/functions/`).5. Clique **Commit changes**.### 2. Connecter le repo 脿 Netlify1. Cr茅e un compte sur [netlify.com](https://netlify.com) (tu peux te connecter avec ton compte GitHub).2. Clique **"Add new site" 鈫� "Import an existing project"**.3. Choisis **GitHub**, autorise l'acc猫s, puis s茅lectionne ton repo `ebook-generator`.4. Netlify d茅tecte automatiquement le fichier `netlify.toml` (dossier `public` + fonctions). Clique **Deploy**.### 3. Ajouter tes cl茅s API (indispensable)1. Dans Netlify, va dans **Site configuration 鈫� Environment variables**.2. Ajoute deux variables :   - `ANTHROPIC_API_KEY` 鈫� ta cl茅 Claude (r茅cup茅rable sur [console.anthropic.com](https://console.anthropic.com))   - `OPENAI_API_KEY` 鈫� ta cl茅 OpenAI (r茅cup茅rable sur [platform.openai.com](https://platform.openai.com))3. Retourne dans **Deploys** et clique **"Trigger deploy" 鈫� "Deploy site"** pour que les cl茅s soient prises en compte.### 4. C'est pr锚t !Ton site est en ligne 脿 une adresse du type `https://ton-site.netlify.app`. Ouvre-la, remplis le formulaire, clique sur **"G茅n茅rer l'ebook"** 鈥� le texte et les illustrations se g茅n猫rent, puis le PDF se t茅l茅charge automatiquement.---## 馃挕 Notes- Chaque g茅n茅ration consomme des cr茅dits sur tes comptes Anthropic et OpenAI (facturation 脿 l'usage).- Le nombre de chapitres est limit茅 脿 12 par g茅n茅ration pour 茅viter les temps d'attente trop longs.- Pour changer le style visuel du PDF (couleurs, polices, mise en page), 